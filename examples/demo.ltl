// examples/demo.ltl — runnable demo for ltl_mbtiles

import std::io
use ltl_mbtiles as lib

fn main() {
    io::println("ltl_mbtiles demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}